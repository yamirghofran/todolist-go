---
description: Describes the structure of the project
globs: 
alwaysApply: false
---
`backend/` is the backend directory where the Go backend project resides. It includes configuration files for sqlc and goose.
`backend/sql/migration` hosts the schema migration files which are managed by goose.
`backend/sql/queries` are the sql queries sqlc will convert to typesafe Go functions.
`backend/internal/models` hosts the models for entities, requests, responses.
`backend/internal/db` hosts the database service layer. Includes files auto-generated by sqlc and service logic by the developer.
`backend/internal/handler` hosts the handler functions which handle the http endpoings.